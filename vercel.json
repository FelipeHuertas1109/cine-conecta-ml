{
  "functions": {
    "api/wsgi.py": {
      "runtime": "python3.12",
      "installCommand": "pip install -r requirements.txt"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/wsgi.py" }
  ]
}
